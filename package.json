{
  "name": "sketchflow",
  "version": "0.1.0",
  "description": "Collects RSS feeds, categorizes them, and generates LLM summaries/discussions.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@google/generative-ai": "^0.16.0",
    "express": "^4.21.2",
    "node-cron": "^4.5.0",
    "rss-parser": "^3.13.0",
    "zod": "^3.23.8"
  }
}
